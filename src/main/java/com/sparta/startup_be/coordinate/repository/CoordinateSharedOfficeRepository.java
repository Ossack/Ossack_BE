package com.sparta.startup_be.coordinate.repository;

import com.sparta.startup_be.model.CoordinateSharedOffice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CoordinateSharedOfficeRepository extends JpaRepository<CoordinateSharedOffice,Long> {
}
